export enum AppRoutes {
  LOGIN = '/auth/login',
  FORGOTTEN_PASSWORD = '/auth/forgotten-password',
  CHANGE_PASSWORD = '/auth/change-password',
  HOME = 'home',
}

export enum ApiRoutes {
  LOGIN = '/account/login',
  FORGOTTEN_PASSWORD = '/account/lost-pass',
  GET_ACCOUNT = '/s/user/get',
}
